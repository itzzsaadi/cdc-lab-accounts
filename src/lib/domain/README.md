# `src/lib/domain`

Placeholder. Pure, framework-agnostic financial calculation logic (funding-source
rule, result calculation, profit split, partner investment aggregation) arrives
in **Phase 1** — see `docs/PROJECT_PLAN.md` and `CLAUDE.md` §6/§7/§12.

No code is added here until Phase 1 begins.
