# `src/lib/validation`

Placeholder. Zod schemas for every write endpoint (one per command/entity,
enforced server-side per `CLAUDE.md` §18) arrive starting **Phase 1/3** —
see `docs/PROJECT_PLAN.md`.

No schemas are added here until then.
