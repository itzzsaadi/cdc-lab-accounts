# `src/lib/auth`

Placeholder. Better Auth configuration, session handling, and the server-side
role-guard helpers (`requireRole`, etc.) arrive in **Phase 2** — see
`docs/PROJECT_PLAN.md` and `CLAUDE.md` §15.

The `better-auth` package is installed as a Phase 0 dependency, but it is not
configured or invoked anywhere yet. No authentication logic is added here
until Phase 2 begins.
