# `src/server`

Placeholder for server-only code (server actions, the Better Auth server
instance, and any other logic that must never be bundled into client code)
that sits alongside `src/lib`. Populated starting **Phase 2** once
authentication and the first real server actions are implemented — see
`docs/PROJECT_PLAN.md`.

No code is added here until then.
